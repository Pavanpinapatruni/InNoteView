[metadata]
name = innoteview
version = 0.0.1
description = Render websites and YouTube videos inside Jupyter Notebooks

[options]
package_dir =
    = src
packages = find:
python_requires = >=3.9
install_requires =
    ipython>=7.0.0

[options.packages.find]
where = src